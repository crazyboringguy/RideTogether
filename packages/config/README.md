# Shared configuration

This directory is reserved for shared tool configuration when duplication emerges. The root
Prettier configuration is sufficient for Phase 1, so no shared configuration package is needed.
