# API contracts

This package is reserved for versioned API schemas and generated client types once the REST API
extends beyond the Phase 1 health endpoint. It deliberately contains no runtime code yet.
