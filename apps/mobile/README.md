# RideTogether mobile app

This is the Flutter source foundation for the RideTogether mobile app. It currently contains
only the branded application shell; trip and location functionality are not implemented.

Native Android and iOS runners are generated. From this directory run:

```powershell
flutter pub get
flutter analyze
flutter test
flutter run
```
