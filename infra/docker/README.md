# Local infrastructure

Docker is not required for the Phase 1 API health endpoint. A future local development setup may
place PostgreSQL and Redis Compose files here. It must source credentials from an uncommitted
environment file and must not define production defaults.
